{
  "roots": ["<rootDir>"],
  "testMatch": ["**/*.test.ts?(x)"],
  "testEnvironment": "jsdom",
  "moduleNameMapper": {
    "\\.(css|less|svg)$": "identity-obj-proxy"
  },
  "setupFilesAfterEnv": ["<rootDir>/jest.setup.ts"],
  "transform": {
    "^.+\\.tsx?$": "ts-jest"
  }
}
